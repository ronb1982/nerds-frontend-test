/**
 * Note: The labels for each link are located in the translation files (e.g "messages/en.json")
 */
export const routes = {
    home: { id: 'home', label: 'Homepage', pathname: '/', protected: false },
}